events {}

http {
    server {
        listen 80;
        location / {
            return 200 'Hello from NGINX - GitOps Sync Success!';
            add_header Content-Type text/plain;
        }
    }
}
